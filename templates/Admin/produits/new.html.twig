{% extends 'base.html.twig' %}

{% block title %}Nouveau Produit - Admin{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <h1 class="mb-4">➕ Nouveau Produit</h1>

                <a href="{{ path('admin_produits') }}" class="btn btn-secondary mb-4">
                    ← Retour à la liste
                </a>

                {{ form_start(form) }}
                <div class="card">
                    <div class="card-header bg-success text-white">
                        <h5 class="mb-0">Création d'un nouveau produit</h5>
                    </div>
                    <div class="card-body">
                        {{ form_widget(form) }}
                    </div>
                    <div class="card-footer">
                        <div class="btn-group">
                            <button type="submit" class="btn btn-primary">
                                💾 Enregistrer
                            </button>
                            <a href="{{ path('admin_produits') }}" class="btn btn-secondary">
                                Annuler
                            </a>
                        </div>
                    </div>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}