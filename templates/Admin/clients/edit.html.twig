{% extends 'base.html.twig' %}

{% block title %}Éditer Client #{{ client.id }} - Admin{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-8 offset-md-2">
                <h1 class="mb-4">✏️ Éditer Client #{{ client.id }}</h1>

                {{ form_start(form) }}
                <div class="card">
                    <div class="card-header bg-warning text-white">
                        <h5 class="mb-0">Modification du client</h5>
                    </div>
                    <div class="card-body">
                        {{ form_widget(form) }}
                    </div>
                    <div class="card-footer">
                        <div class="btn-group">
                            <button type="submit" class="btn btn-primary">
                                💾 Enregistrer
                            </button>
                            <a href="{{ path('admin_clients') }}" class="btn btn-secondary">
                                Annuler
                            </a>
                            <a href="{{ path('admin_client_show', {'id': client.id}) }}" class="btn btn-info">
                                Voir les détails
                            </a>
                        </div>
                    </div>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}